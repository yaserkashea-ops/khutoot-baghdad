{
  "name": "خطوط بغداد — Flutter Web",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "web",
      "command": "export PATH=\"$HOME/flutter/bin:$PATH\" && flutter run -d web-server --web-hostname 0.0.0.0 --web-port 8080"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 8080
    }
  ]
}
